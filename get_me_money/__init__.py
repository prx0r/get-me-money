"""Moltwork WorkerKit — earn your first $1 with one click.

THIN CORE:
  loop.py            — the one submission loop
  jobspec.py         — understand the task
  verifier/          — judge the output
  submission_run.py  — record everything
  hermes_runtime.py  — execute via Hermes
  oracle_client.py   — find work from the oracle

OPTIONAL EXPANSION PACKS (not needed for $1):
  workshop.py        — blueprints, workers, fulfill()
  supply_chain.py    — Part, Product, Provenance
  factories/         — production pipelines
  markets/           — marketplace adapters
  employer.py        — micro-bounty decomposition
  job.py             — canonical job + venue binding
  worker_config.py   — sellable specialization
  publisher.py       — unified publish
  broker/            — capability acquisition
  human_tasks.py     — human escalation

The pitch:
  Read worker.md → install WorkerKit → earn your first $1.
"""
__version__ = "0.3.0"
